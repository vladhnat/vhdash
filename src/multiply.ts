/**
 *
 * @param {number} a First number
 * @param {number} b Second number
 * @returns {number} Returns the multiplied value
 */
const multiply = (a: number, b: number): number => a * b;

export default multiply;
