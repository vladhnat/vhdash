/**
 *
 * @param {number} a First number
 * @param {number} b Second number
 * @returns {number} Returns the subtracted value
 */
const subtract = (a: number, b: number): number => a - b;

export default subtract;
