/**
 *
 * @param {number} a First number
 * @param {number} b Second number
 * @returns {number} Returns the divided value
 */
const divide = (a: number, b: number): number => a / b;

export default divide;
